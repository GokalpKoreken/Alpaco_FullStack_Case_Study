# DropSpot Frontend (minimal)

This is a minimal React frontend to exercise the backend endpoints.

Run:

1. cd frontend
2. npm install
3. npm start

It contacts backend at http://localhost:4000 by default.
